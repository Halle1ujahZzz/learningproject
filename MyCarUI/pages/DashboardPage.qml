import QtQuick
import QtQuick.Controls

Page {
    background: Rectangle { color: "#121212" }

    Label {
        anchors.centerIn: parent
        text: "仪表盘"
        color: "white"
        font.pixelSize: 80
        font.bold: true
    }
}
